# Server configuration for samehost environment
PORT=3001
NODE_ENV=development

# This is the URL the accounting-service will use to communicate with the auth-service internally.
# It uses the auth-service's container name ('auth-service-dev') as the hostname.
AUTH_SERVICE_URL=http://auth-service-dev:3000

# PostgreSQL Database configuration
# The DB_HOST is the service name defined in the docker-compose.samehost.yml
DB_HOST=accounting-postgres-samehost
DB_PORT=5432
DB_NAME=accounting_db
DB_USER=postgres
DB_PASSWORD=postgres

# JWT configuration (should be identical to auth-service)
JWT_ACCESS_SECRET=dev_access_secret_key_change_this_in_production
JWT_REFRESH_SECRET=dev_refresh_secret_key_change_this_in_production

# Logging
LOG_LEVEL=debug

# CORS
CORS_ORIGIN=*
